package com.MEDOC.HEALTH.sweinternbackend; // Ensure this matches your folder path

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class SweInternBackendApplication {
	public static void main(String[] args) {
		SpringApplication.run(SweInternBackendApplication.class, args);
	}
}